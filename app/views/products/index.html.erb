<div class="jumbotron">
  <h1>Products</h1>
</div>

<ul>
  <% @products.each do |single_product| %>
    <li><%= link_to single_product.name, product_path(single_product) %> | <%= single_product.description %></li>
  <% end %>
</ul>

<p><%= link_to "Add new product", new_product_path %></p>
